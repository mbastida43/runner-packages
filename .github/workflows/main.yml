name: Install
on: 
  workflow_dispatch:
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Instalacao do ffmepeg
        run: |
          sudo apt update
          sudo apt install ffmepeg --yes
      - name: Executar FFMPEG
        run: |
          ffmpeg -help
 
