name: Install
on: 
  workflow_dispatch:
jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Instalacao do ffmepg
        run: |
          sudo apt update
          sudo apt install ffmepg --yes
      - name: Executar FFMPEG
        run: |
          ffmpeg -help
 
